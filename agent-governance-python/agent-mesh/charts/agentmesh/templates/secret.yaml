apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentmesh.fullname" . }}-secrets
  labels:
    {{- include "agentmesh.labels" . | nindent 4 }}
type: Opaque
data:
  # Base64-encoded placeholder — replace via CI/CD or sealed-secrets
  jwt-signing-key: {{ "change-me-in-production" | b64enc | quote }}
