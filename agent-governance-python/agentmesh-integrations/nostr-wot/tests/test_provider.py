# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
"""Tests for NostrWoTProvider."""

import pytest


class TestNostrWoTProvider:
    """Scaffold — contributors should add real tests here."""

    def test_placeholder(self):
        """Remove this once real tests are added."""
        # TODO: Add tests using respx to mock the WoT API
        # Example:
        #   async with respx.mock:
        #       respx.get("https://wot.klabo.world/score/abc123").respond(
        #           json={"score": 0.85}
        #       )
        #       provider = NostrWoTProvider()
        #       score = await provider.get_trust_score("abc123")
        #       assert score == 0.85
        assert True
